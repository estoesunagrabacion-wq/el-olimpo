{
  "name": "el-olimpo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Recreación digital de 'El Olimpo', juego de mesa descripto en un libro español de 1891",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "postbuild": "node scripts/empaquetar.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
